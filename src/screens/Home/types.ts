export interface Font  {
    fontName: string;
    url: string;
    featuredCarousel: boolean;
    featuredThumbnial: boolean;
    controls: controls

}

interface Controls {
    optionOne: boolean;
}